# Disable directory browsing
Options -Indexes

# Enable URL rewriting
RewriteEngine On

# Send request to index.php unless it's a real file or folder
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php/$1 [L]
